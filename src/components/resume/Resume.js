import React from 'react';
import Footer from '../footer/Footer';
import { Document, Page } from 'react-pdf'
import resume from '../images/Resume - Google Docs.pdf'


const Resume = () => {
    return (
        <div>
            Resume
            <div><Footer /></div>
        </div>
    );
};

export default Resume;